# Task 10.3 — Flyway + MySQL Initialization Smoke Test Report

Requirements: 1.3, 2.4, 4.3, 5.4, 7.1, 7.2, 7.3

## Environment
- MySQL client: `D:\mysql\bin\mysql.exe Ver 8.1.0 for Win64` (MySQL 8.x server reachable on `127.0.0.1:3306`).
- Dev credentials `aws`/`aws` were initially absent; provisioned the `aws` user and an empty `adpilot` database via root, then ran the migrations as `aws`.

## What was verified
A full Flyway-managed backend startup could NOT complete because the backend currently has 12 pre-existing **compilation errors** (missing classes: `ReturnMapper`, `RefundMapper`, `SettlementMapper`, `BuyerMessageMapper`, `SupplierVo`, `SupplierDto`). These are unrelated to the migration work and belong to task 10.1 (backend build verification). The app therefore never reaches the Flyway/JPA-validate phase via `spring-boot:run`.

To still validate the migrations end-to-end against a real MySQL 8.0 engine, the consolidated migration SQL was executed directly against the empty `adpilot` database:

| Check | Result |
|---|---|
| Empty `adpilot` DB charset/collation | `utf8mb4` / `utf8mb4_unicode_ci` — PASS |
| Apply `V1__init_schema.sql` | Exit 0, no MySQL syntax errors — PASS |
| Apply `V2__seed_data.sql` | Exit 0, no MySQL syntax errors — PASS |
| Total tables created | 109 |
| Collision fix: `listing_monitors` + `listing_quality_checks` both present & distinct | PASS |
| Six previously-missing tables (`login_logs`, `purchase_requests`, `cash_flow`, `receivables`, `payables`, `customer_tickets`) | All present — PASS |
| `purchase_requests` distinct from `purchase_orders` | PASS |
| `data_scopes` JSON columns (`store_ids`, `product_ids`) | `json` type — PASS |
| `data_scopes` seed rows | 13 rows; `scope_type` mapped to `all_company` / `assigned_store` / `department` (no legacy `all`/`assigned`) — PASS |

This confirms: no Flyway/SQL syntax errors and no missing-table errors against MySQL 8.0. The JPA `validate` phase could not be exercised live due to the compile failure above.

## Fallback per Req 7.3
Also re-ran the static validation and doc checks:
- Forbidden PostgreSQL constructs scan (`TIMESTAMPTZ`, `JSONB`, `uuid_generate_v4`, `uuid-ossp`, `pgcrypto`, `CREATE EXTENSION`, `NUMERIC(`, `ON CONFLICT`, `CREATE INDEX IF NOT EXISTS`): zero actual occurrences in both files (single `JSONB` hit is a comment line in V1 header). PASS.
- Both files use valid Flyway versioned naming (`V1__`, `V2__`) with no duplicate versions. PASS.
- README contains the "本地 Flyway 验证 (Req 7.3 fallback)" section documenting create-DB, JDBC URL, start command, and expected `flyway_schema_history` + `success` results. PASS.

## Cleanup
- Dropped the `adpilot` smoke-test database. Temp setup script removed. (The `aws` MySQL user was left in place.)

## Caveat
The live Flyway-orchestrated startup + JPA `validate` path remains blocked by the backend compile errors (task 10.1). Once those are resolved, re-run `.\mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=dev` against an empty `adpilot` to confirm `flyway_schema_history` rows and clean JPA validation in the real boot sequence.
