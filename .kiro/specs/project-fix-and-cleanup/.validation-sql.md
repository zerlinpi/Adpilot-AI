# Task 2.5 — Static SQL Validation Report

Scope: `backend-java/src/main/resources/db/migration/V1__init_schema.sql` and `V2__seed_data.sql`
Requirements: 2.2, 2.4, 2.5

## 1. Forbidden construct scan (case-insensitive)

| Pattern | V1 | V2 | Verdict |
|---|---|---|---|
| `TIMESTAMPTZ` | 0 | 0 | PASS |
| `JSONB` | 1 (comment only) | 0 | PASS |
| `uuid_generate_v4` | 0 | 0 | PASS |
| `uuid-ossp` | 0 | 0 | PASS |
| `pgcrypto` | 0 | 0 | PASS |
| `CREATE EXTENSION` | 0 | 0 | PASS |
| `NUMERIC(` | 0 | 0 | PASS |
| `ON CONFLICT` | 0 | 0 | PASS |
| `CREATE INDEX IF NOT EXISTS` | 0 | 0 | PASS |

### Note on the single `JSONB` match
The only match is in the V1 header comment block (line 7):

```
--   * jsonb -> JSON
```

This is prose documenting the PostgreSQL→MySQL normalization that was applied
(jsonb was replaced with the MySQL `JSON` type). It is not an SQL construct.
**Zero actual forbidden SQL constructs are present in either file.**

## 2. Flyway versioned naming

Files in `db/migration`:
- `V1__init_schema.sql`
- `V2__seed_data.sql`

Both follow Flyway versioned naming (`V<n>__<description>.sql`). Version numbers
1 and 2 are unique — **no duplicate version numbers on the classpath**. PASS.

## Result
All acceptance criteria satisfied. No code changes required.
